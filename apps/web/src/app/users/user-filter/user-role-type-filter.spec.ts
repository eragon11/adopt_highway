import { UserRoleTypeFilter } from '../models/user-role-type-filter';

describe('UserRoleTypeFilter', () => {
  it('should create an instance', () => {
    expect(new UserRoleTypeFilter()).toBeTruthy();
  });
});
